# Sprint 7A.1 local Playwright screenshots

22 PNG trong thư mục này được chụp bằng Playwright 1.51.1 với Chromium/Google
Chrome 150.0.7871.186 trên runtime loopback của PROJECT004 ngày 2026-08-01.

- Không dùng in-app Browser, connected browser hoặc ảnh giả.
- Chỉ dùng fixture local tổng hợp; không có PII, UUID, email, token hoặc allowlist.
- Phần đáp án/lời giải trong ảnh feedback được mask; danh sách review từng câu
  được loại khỏi ảnh completion để giữ private-solution boundary.
- Mỗi ảnh đã được mở bằng local image inspection. Sau bốn vòng chụp–sửa–chụp
  lại, không còn issue visual severity critical/high.

Danh sách, trạng thái review và browser metrics đầy đủ nằm tại `../report.json`;
raw Playwright result nằm tại `../playwright-result.json`.
