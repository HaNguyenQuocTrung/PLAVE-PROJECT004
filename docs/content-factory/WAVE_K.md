# Wave K — final deterministic curriculum gap closure

Wave K rebuilds the remaining inventory from the retained MOET source map and immutable combined A–J packs. It audits 345 previously reported source-verified/structured outcomes plus the 41 already-known experiential outcomes, without assuming that every row belongs in an exact-answer candidate.

## Accounting boundary

- `PRODUCIBLE_DETERMINISTIC`: 149 skills. Each receives exactly six source-bound cases because the immutable Wave I attempt limit and Wave J pool contract require six unseen items, at least two reasoning structures and at least three bounded-session public forms.
- `ALREADY_COVERED_SEMANTICALLY`: 1 exact Grade 9 alias; the source meaning and grade scope are identical and the target remains recorded.
- `AUTOMATED_VERIFICATION_INSUFFICIENT`: 115 outcomes whose requested process, proof/construction semantics, calculator use, representation judgment or factual context cannot be observed or independently reconstructed by the available oracle. This includes 103 conservative downgrades found when broad mathematical templates were checked against exact source meaning.
- `OPEN_ENDED_OR_EXPERIENTIAL`: 61 outcomes, including the 41 experiential rows already excluded from the reported remaining counts.
- `VISUAL_EVIDENCE_REQUIRED`: 59 outcomes for which the retained candidate contract lacks an independently verifiable diagram, chart, construction or image channel.
- `UNKNOWN_SOURCE_MAPPING`: 1 outcome remains fail-closed rather than inferred.

The resulting 894 questions are generated by deterministic grade shards and independently recomputed from typed oracle payloads. Wave K does not copy textbook exercises and does not claim expert pedagogical validation.

## Grade 1 immutable audit

Grade 1 remains `13 / 312 / 312 / 24`, with no new question and no SQL, prompt, answer, solution, ID, runtime, source-digest or shadow-tuple change. The final evidence report records 84 independently evidenced legacy items, 24 explicitly quarantined visual/unknown items and 228 total UNKNOWN items. Evidence coverage is therefore intentionally not declared complete.

## Release and progression boundary

Every Wave K and combined A–K candidate remains `DRAFT/HIDDEN`; publication, pilot, runtime and retention flags are false and no default entitlement is created. New prerequisite edges are explicitly `HYPOTHESIS_REQUIRES_EVIDENCE`, never promoted to curriculum fact. Wave I remediation, retry, return, retention, mixed-practice and fail-closed contracts remain in force without changing `schoolGrade` or granting entitlement.

Wave F's historical DNS-attempt incident remains preserved. Wave K uses verified local executables under offline mode and records zero new network attempts. One Wave K operational incident with two occurrences is retained without rewriting: two secret-boundary invocations read a configured local key value solely to test whether it was nonempty and then failed closed before value output, provider use or network activity. The sanitized equivalent then passed with all local environment files hidden and restored.
