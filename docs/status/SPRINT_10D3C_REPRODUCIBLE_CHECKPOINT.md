# Sprint 10D.3C — Reproducible local checkpoint

Date: 2026-08-03

## Authorized operation

The Owner authorized exactly one local checkpoint commit for Sprint 10D.3.
Push, amend, merge, rebase and tag remain prohibited. The checkpoint is scoped
to the final independent Oracle interaction and exact numeric canonicalization
remediation; it does not activate Generator V2 by default or close Milestone 2.

## Preflight

- Branch: `main`.
- Previous HEAD and canonical parent:
  `b5eeabab22f0183ceef1bd64cc1d94d34e882cfd`.
- Local `main` was 4 commits ahead and 0 behind `origin/main` at
  `37fc040b9854867d8b04e4f5bf8de8beda9121ef`.
- Staged files before the operation: 0; Git lock files: 0.
- Owner Tutor remained listening on `127.0.0.1:3001` and was neither inspected
  beyond listener metadata nor terminated.
- `.env.local` remained ignored, unread and mode 0600.
- The modified Google real-smoke artifact and historical 10D/10D.2 evidence
  remained unstaged and unread where prohibited.

## Checkpoint boundary

Included:

- independent `BigInt` exact numeric parser and canonical key;
- interaction/answer-kind Oracle diagnostic;
- 11 exact outcome-ID fraction-representation exceptions;
- exact regression, record-derived falsification and public-runtime boundary
  tests;
- Sprint 10D.3 status, machine evidence, reviewed screenshots and this
  reproducibility contract.

Excluded:

- `.env.local`, provider keys, caches, browser profiles, temporary databases,
  private-solution artifacts and identity fixtures;
- `artifacts/ai-tutor-acceptance/google-real-smoke.json`;
- historical Sprint 10D and Sprint 10D.2 screenshots/evidence;
- post-commit Sprint 10D.1C clean-checkout evidence and other unrelated state.

## Explicit exception audit

The exception registry contains exactly 11 canonical outcome IDs. Each ID has
an explicit fraction/rational representation reason, exists in the typed
outcome registry and is exercised by the 32,760-coordinate interaction matrix.
There is no wildcard, regex, capability-wide or grade-wide exception. The
original failing Grade 7 algebra outcome `MOET2018-G7-NAA-P057-030` is not an
exception. The matrix observed 126 authorized denominator-one fraction samples
and zero unauthorized denominator-one `FRACTION_INPUT` samples.

## Legacy evidence drift preserved

`npm run --silent audit:generator-v2-full-coverage` remains present in
`package.json` and is also invoked by the historical
`test:generation-v2-full-coverage` alias. No repository CI workflow directory
is present. That superseded command reports one exact duplicate for
`MOET2018-G6-STA-P054-009/HARD` in its alternate `sprint8c` seed namespace.
It is neither deleted nor modified in this checkpoint. The canonical current
correctness command is
`npm run --silent audit:generator-v2-independent-oracle`; classification of
the legacy alias is deferred to Sprint 10D.4.

## Validation contract

Before the commit, a temporary tree materialized from the Git index only must
pass exact regressions, 34/34 falsification records, the Oracle dependency
boundary, 32,760/32,760 correctness, denominator-one inventory, Student
runtime, Practice, AI Tutor key-unset, typecheck, lint, 77-page build, secret
canary, JSON validation and npm audit when the registry is available.

The final staged-only result is recorded in
`artifacts/remediation/sprint-10d3c-checkpoint.json`. The commit uses
`SELF_COMMIT` because a commit cannot embed its own hash. The exact SHA and
post-commit clean-worktree results are recorded outside that single commit in
`artifacts/remediation/sprint-10d3c-clean-checkout.json`.

Final staged-only result: `PASS`. The index-only snapshot used a fresh lockfile
install and passed the 3 new exact regressions, combined Oracle suite 11/11,
falsification 34/34 (16 invalid mutations killed, 18 valid equivalences
accepted, zero unexpected survivors), dependency boundary and mutations 12/12,
full correctness 32,760/32,760, authenticated public Student runtime 198/198,
Practice 550/550, AI Tutor 40/40 key-unset, typecheck, lint, the 77-page build,
289/289 JSON parses, isolated secret canary and npm audit with 0
vulnerabilities. The five PTY-dependent AI Tutor configure tests first received
the expected sandbox `/dev/tty` denial, then passed on the authorized host PTY;
no provider request was made.

Milestone 2 remains `REOPENED_AWAITING_SPRINT_10D4_REAUDIT`.
