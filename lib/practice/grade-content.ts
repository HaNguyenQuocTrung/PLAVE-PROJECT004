export function getGradeContentEmptyTitle(grade: number) {
  return `Nội dung Toán Lớp ${grade} đang được chuẩn bị.`;
}

export function getGradeContentEmptyDescription() {
  return "PLAVE sẽ hiển thị bài học khi nội dung của khối lớp này đã sẵn sàng và được kiểm tra kỹ.";
}
